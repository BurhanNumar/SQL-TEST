/*USE ETRADE2

/*SELECT COUNT (*) AS COUNTOFJOIN FROM (
SELECT ITEMS.CATEGORY1,ITEMS.CATEGORY2,ITEMS.ID,ITEMS.ITEMNAME FROM ITEMS 
LEFT JOIN SALES ON ITEMS.CATEGORY1=SALES.CATEGORY1)
AS TABLE_JOIN; */

SELECT COUNT(*) AS COUNTOFITEMS  FROM ITEMS
SELECT COUNT (*) AS COUNTOFSALES FROM SALES */

--TOPLAMA İŞLEMİ YAPILACAK DEĞİŞKENLERİN TANIMLANMASI

DECLARE @NUMBEROFITEMS BIGINT ;
DECLARE @NUMBEROFSALES BIGINT;
--TANIMLANAN DEĞİŞKENİN SORGU ÇEKİLEREK İÇERİSİNE DEĞER ATANMASI
SELECT @NUMBEROFITEMS= COUNT(*) FROM ITEMS;  
--TANIMLANAN DEĞİŞKENİN SORGU ÇEKİLEREK İÇERİSİNE DEĞER ATANMASI
SELECT @NUMBEROFSALES= COUNT(*) FROM SALES;

PRINT 'SATIŞ KAYIT SAYISI: ' + CAST (@NUMBEROFSALES AS VARCHAR(10))+		  
 ' ITEM KAYIT SAYISI:  ' + CAST (@NUMBEROFITEMS AS VARCHAR(10));
 PRINT @NUMBEROFSALES*@
